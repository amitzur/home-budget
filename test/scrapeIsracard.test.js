'use strict';

const {describe, it} = require('mocha');
const {expect} = require('chai');
const makeScrapeIsracard = require('../src/scrapeIsracard');

describe('scrapeIsracard', () => {
  it('works', async () => {});
});
