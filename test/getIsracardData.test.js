'use strict';

const {describe, it} = require('mocha');
const {expect} = require('chai');
const makeGetIsracardData = require('../src/getIsracardData');

describe('getIsracardData', () => {
  it('works', async () => {});
});
